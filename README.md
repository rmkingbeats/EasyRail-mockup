# EasyRail

A polished, independent concept for a calmer Indian rail travel experience. This prototype uses mock content only and does not connect to IRCTC or any railway API.

## Run locally

```bash
npm install
npm run dev
```

The landing page is available at `/`. Placeholder routes are included for `/book`, `/pnr`, `/tickets`, `/wallet`, `/account`, and `/food`.
